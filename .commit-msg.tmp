feat(pricing): wire Watchlist / Dealer subscribe CTAs to Stripe (guest flow)

- New /subscribe/[tier] dynamic page with dedicated SubscribeCheckoutForm
  client component — collects email + monthly/yearly, hands off to Stripe
- Pricing page CTAs updated:
    Watchlist    /auth?tier=watchlist   → /subscribe/watchlist
    Dealer Edge  /dealer?plan=edge      → /subscribe/dealer-edge
    Inventory IQ /dealer?plan=inventory → /subscribe/inventory-iq
    Free         /auth                  → /cars (dead link fix)
- lib/api.ts: startGuestSubscriptionCheckout posts to new backend route
- Matches backend canonical 5-SKU catalogue verbatim (pricing drift guarded
  by single source of truth in config/stripe.ts on the backend)
- Graceful degradation preserved: unconfigured API_BASE keeps CTAs disabled
  with an inline "coming soon" message rather than broken redirects
